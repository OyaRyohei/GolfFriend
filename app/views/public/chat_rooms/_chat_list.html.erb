<!--% messages.each do |chat| %>-->
  <!--<div id="message_%=chat.id%>">-->
  <!--% if chat.user_id == current_user.id %>-->
<!--    <p style="text-align: left;">%= chat.body %></p>-->
<!--  <%# else %>-->
<!--    <p style="text-align: right;"><%#= chat.body %></p>-->
<!--  <%# end %>-->
<!--  </div>-->
<!--<%# end %>-->

<table class="message table">
  <thead>
    <tr>
      <th style="text-align: left; font-size: 20px;"><%= current_user.name %></th>
      <th style="text-align: right; font-size: 20px;"><%= partner.name %></th>
    </tr>
  </thead>
  <% chat_room.chat_messages.each do |chat| %>
    <% if chat.user_id == current_user.id %>
    <tbody>
      <tr>
        <th>
          <p style="text-align: left;"><%= chat.body %></p>
        </th>
        <th></th>
      </tr>
    <% else %>
      <tr>
        <th></th>
        <th>
          <p style="text-align: right;"><%= chat.body %></p>
        </th>
      </tr>
    </tbody>
    <% end %>
  <% end %>
</table>
